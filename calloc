#include <stdio.h>
#include <malloc.h>

int main()
{
	int i, *ptr,size;
	printf("enter size:");
	scanf("%d",&size);
	ptr=(int*)(calloc(size,sizeof(int)));
	printf("values are:");
	for(i=0;i<size;i++)
	{
		printf("%d",arr[i]);
	}
	printf("enter newsize:");
	for(i=0;i<size;i++)
	{
		scanf("%d",&arr[i]);
	}
printf("\nnew values:");
	for(i=0;i<size;i++)
	{
		printf("%d",arr[i]);
	}

	return 0;
}
